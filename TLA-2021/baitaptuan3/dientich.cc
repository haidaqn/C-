#include <bits/stdc++.h>

using namespace std;

int main() {

    long double a,b,s;

    cin>>a>>b;

    s= 2*a*b + (3.14*a*a)/2;;
    
    cout << fixed << setprecision(4) << s << endl;
    
    return 0;
}